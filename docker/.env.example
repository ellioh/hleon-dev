# Variables de docker-compose.yml - independientes de backend/laravel/.env
# (aunque DB_DATABASE/DB_USERNAME/DB_PASSWORD deben coincidir con lo que
# Laravel espera cuando DB_CONNECTION=mysql).
DB_DATABASE=hleon_dev
DB_USERNAME=
DB_PASSWORD=
DB_ROOT_PASSWORD=
NGINX_PORT=8080
